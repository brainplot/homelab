- name: Configure enabled locales
  become: true
  ansible.builtin.copy:
    dest: /etc/locale.gen
    mode: '0644'
    content: |
      C.UTF-8 UTF-8
      {{ lang }} UTF-8
  notify:
    - Generate locales
    - Set default system locale
